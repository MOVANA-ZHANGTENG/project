# 四向车与提升机同步方案

## 一、需求分析

### 1.1 背景
- `1-8-3`, `2-8-3`, `3-8-3` 是同一个提升机的3层位置（贯穿提升机）
- 当四向车的路径（`rcsCarPath`）终点是这些位置时，提升机需要提前移动到相应层
- 提升机位置在四向车下方，Y坐标根据库位空物体位置确定

### 1.2 核心思路

**需求一：基于路径终点检测，提前移动提升机**
- 如果 `rcsCarPath` 的 `toCellCode` 是 `1-8-3`、`2-8-3`、`3-8-3` 中的任意一个
- 说明四向车要到达这个位置，提升机需要提前移动到相应层
- 提升机位置 = 库位位置的Y值（在四向车下方）

**需求二：四向车跟随提升机移动**
- 请求后端接口，获取提升机位置变化信息
- 如果提升机位置发生变化（上升或下降）
- 检查当前是否有四向车在提升机位置（`1-8-3`、`2-8-3`、`3-8-3`）
- 如果有，四向车需要跟随提升机一起上升或下降（Y坐标同步变化）

### 1.3 关键问题
1. **路径检测**：如何检测 `rcsCarPath` 的终点是否是提升机位置
2. **层数映射**：如何根据库位编码确定层数（1-8-3 → 第1层，2-8-3 → 第2层，3-8-3 → 第3层）
3. **提升机定位**：如何确定提升机对象，以及如何设置其位置
4. **提前移动**：提升机需要提前移动到目标层，等待四向车到达
5. **位置计算**：提升机的Y坐标如何根据库位空物体位置计算
6. **后端接口**：如何请求后端接口获取提升机位置变化
7. **跟随移动**：如何检测四向车在提升机位置，并同步Y坐标变化

## 二、方案设计（基于路径终点检测）

### 2.1 核心思路

**检测路径终点，提前移动提升机**：
- 在 `loadCarPaths` 方法中，处理 `rcsCarPath` 数据时
- 检查每条路径的 `toCellCode` 是否是提升机位置（`1-8-3`、`2-8-3`、`3-8-3`）
- 如果是，提取路径的最后一个 `toCellCode`（即最终目的地）
- 提升机提前移动到对应层，位置Y值 = 库位空物体的Y值（在四向车下方）

### 2.2 数据结构设计

```javascript
// 提升机配置（只有一个提升机）
elevatorConfig: {
  // 提升机位置映射：同一提升机的不同层位置
  positions: ['1-8-3', '2-8-3', '3-8-3'],  // 提升机的3层位置
  layerMap: {
    '1-8-3': 1,  // 第1层
    '2-8-3': 2,  // 第2层
    '3-8-3': 3   // 第3层
  },
  elevatorName: '提升机',  // 提升机对象名称（已确认）
  heightOffset: -2.0,       // 提升机在四向车下方的偏移量（负数表示在下方，可调整）
}

// 提升机状态管理
elevatorState: {
  currentLayer: null,        // 当前所在层（1-3），null表示未初始化
  targetLayer: null,         // 目标层（移动时）
  isMoving: false,           // 是否正在移动
  targetCellCode: null,      // 目标库位编码（如 '1-8-3'）
  currentPosition: null,     // 当前位置 {x, y, z}
  targetPosition: null,      // 目标位置 {x, y, z}
}
```

### 2.3 检测逻辑

#### 2.3.1 路径终点检测
在 `loadCarPaths` 方法中，处理路径数据时：
1. 按 `rcsCarId` 分组路径后，对每个路径组进行处理
2. 找到路径组的最后一条路径（即最终目的地）
3. 检查最后一条路径的 `toCellCode` 是否是提升机位置
4. 如果是，触发提升机移动到对应层

#### 2.3.2 检测实现
```javascript
// 在 renderCarPath 或 loadCarPaths 中检测路径终点
function checkPathDestination(pathList) {
  if (!pathList || pathList.length === 0) {
    return null;
  }
  
  // 获取路径的最后一条（最终目的地）
  const lastPath = pathList[pathList.length - 1];
  const destinationCellCode = lastPath.toCellCode;
  
  if (!destinationCellCode) {
    return null;
  }
  
  // 检查是否是提升机位置（只有一个提升机）
  if (this.elevatorConfig.positions.includes(destinationCellCode)) {
    return {
      cellCode: destinationCellCode,
      layer: this.elevatorConfig.layerMap[destinationCellCode]
    };
  }
  
  return null;
}
```

### 2.4 提升机位置计算

#### 2.4.1 位置计算逻辑
```javascript
function calculateElevatorPosition(targetCellCode) {
  // 1. 获取库位空物体的位置
  const cellLocation = this.getStorageLocationByCode(targetCellCode);
  if (!cellLocation) {
    console.warn(`未找到库位位置: ${targetCellCode}`);
    return null;
  }
  
  // 2. 计算提升机位置（在四向车下方）
  const elevatorPosition = {
    x: cellLocation.x,  // X坐标与库位相同
    y: cellLocation.y + this.elevatorConfig.heightOffset,  // Y坐标 = 库位Y + 偏移量（负数）
    z: cellLocation.z   // Z坐标与库位相同
  };
  
  return elevatorPosition;
}
```

#### 2.4.2 提升机移动动画
```javascript
function moveElevatorToPosition(targetPosition, targetCellCode) {
  // 获取提升机对象
  const elevatorObject = this.ThreeEngine.getObjectByName(this.elevatorConfig.elevatorName);
  if (!elevatorObject) {
    console.warn(`未找到提升机对象: ${this.elevatorConfig.elevatorName}`);
    return;
  }
  
  // 如果正在移动，停止当前动画
  if (this.elevatorState.isMoving && elevatorObject.tweenAnimation) {
    elevatorObject.tweenAnimation.stop();
  }
  
  this.elevatorState.isMoving = true;
  this.elevatorState.targetPosition = targetPosition;
  this.elevatorState.targetCellCode = targetCellCode;
  
  // 获取当前位置
  const currentPos = {
    x: elevatorObject.position.x,
    y: elevatorObject.position.y,
    z: elevatorObject.position.z
  };
  
  // 创建移动动画（与四向车动画时间一致：2秒）
  const tweenObj = { ...currentPos };
  elevatorObject.tweenAnimation = new TWEEN.Tween(tweenObj)
    .to(targetPosition, 2000) // 2秒动画
    .easing(TWEEN.Easing.Quadratic.InOut)
    .onUpdate(() => {
      elevatorObject.position.set(tweenObj.x, tweenObj.y, tweenObj.z);
    })
    .onComplete(() => {
      elevatorObject.position.set(targetPosition.x, targetPosition.y, targetPosition.z);
      this.elevatorState.currentPosition = { ...targetPosition };
      this.elevatorState.currentLayer = this.elevatorConfig.layerMap[targetCellCode];
      this.elevatorState.isMoving = false;
      this.elevatorState.targetLayer = null;
      elevatorObject.tweenAnimation = null;
      console.log(`提升机已移动到 ${targetCellCode} (第${this.elevatorState.currentLayer}层)`);
    })
    .start();
}
```

### 2.5 同步策略

#### 2.5.1 场景分析

**场景1：路径终点是提升机位置**
- 路径数据：`[{fromCellCode: '1-1-1', toCellCode: '1-8-3'}, ...]`
- 最后一条路径的 `toCellCode` 是 `1-8-3`（第1层）
- **动作**：提升机立即移动到第1层，位置Y = 库位 `1-8-3` 的Y值 + heightOffset

**场景2：路径终点不是提升机位置**
- 路径数据：`[{fromCellCode: '1-1-1', toCellCode: '1-2-3'}, ...]`
- 最后一条路径的 `toCellCode` 不是提升机位置
- **动作**：提升机不移动，保持当前位置

**场景3：多个四向车路径终点是不同层**
- 四向车A的路径终点：`1-8-3`（第1层）
- 四向车B的路径终点：`3-8-3`（第3层）
- **动作**：提升机移动到最后一个请求的层（或根据优先级）

**场景4：路径更新，终点改变**
- 之前路径终点：`1-8-3`（第1层）
- 新路径终点：`2-8-3`（第2层）
- **动作**：提升机从第1层移动到第2层

#### 2.5.2 同步算法

```javascript
// 在 loadCarPaths 方法中，处理完路径后检测
function syncElevatorWithPaths(pathGroups) {
  // 遍历所有路径组，找出终点是提升机位置的
  let targetDestination = null; // 记录目标位置（如果有多个四向车，使用最后一个）
  
  Object.keys(pathGroups).forEach(rcsCarId => {
    const pathList = pathGroups[rcsCarId];
    const destination = this.checkPathDestination(pathList);
    
    if (destination) {
      // 记录目标（如果有多个，使用最后一个）
      targetDestination = {
        cellCode: destination.cellCode,
        layer: destination.layer,
        rcsCarId: rcsCarId
      };
    }
  });
  
  // 如果找到目标位置，移动提升机
  if (targetDestination) {
    const state = this.elevatorState;
    
    // 如果目标层与当前层不同，或者还未初始化
    if (state.currentLayer !== targetDestination.layer || state.currentLayer === null) {
      // 计算提升机目标位置
      const targetPosition = this.calculateElevatorPosition(targetDestination.cellCode);
      
      if (targetPosition) {
        this.moveElevatorToPosition(targetPosition, targetDestination.cellCode);
      }
    } else {
      // 已经在目标层，无需移动
      console.log(`提升机已在第 ${targetDestination.layer} 层，无需移动`);
    }
  }
}
```

### 2.7 后端接口集成（需求二）

#### 2.7.1 接口设计
```javascript
// 假设后端接口：getElevatorStatus
// 返回格式：
{
  code: 200,
  data: {
    elevatorId: 'elevator-1',
    currentLayer: 2,        // 当前层（1-3）
    targetLayer: 3,          // 目标层（如果正在移动）
    cellCode: '2-8-3',      // 当前库位编码
    isMoving: false,         // 是否正在移动
    position: {             // 当前位置（可选）
      x: 100,
      y: 200,
      z: 300
    }
  }
}
```

#### 2.7.2 接口请求逻辑
```javascript
// 定时请求提升机状态（建议每1-2秒）
async loadElevatorStatus() {
  try {
    const response = await getElevatorStatus({
      wareCode: 'sxc'
      // 注意：如果后端需要 elevatorId，可以传入，但只有一个提升机，可能不需要
    });
    
    if (response.code !== 200) {
      console.error('查询提升机状态失败:', response.msg);
      return null;
    }
    
    return response.data;
  } catch (error) {
    console.error('请求提升机状态失败:', error);
    return null;
  }
}
```

#### 2.7.3 检测四向车在提升机位置
```javascript
function findVehiclesAtElevator(cellCode) {
  const vehiclesAtElevator = [];
  
  // 遍历所有四向车，检查是否在提升机位置
  // 需要从 lastVehicleData 或当前四向车数据中获取
  if (this.lastVehicleData && this.lastVehicleData.data) {
    this.lastVehicleData.data.forEach(car => {
      // 检查四向车是否在提升机位置
      // 情况1：静止在提升机位置（fromCellCode 是提升机位置，toCellCode 为空或相同）
      if (car.fromCellCode === cellCode && (!car.toCellCode || car.toCellCode === cellCode)) {
        vehiclesAtElevator.push({
          code: car.code,
          vehicleGroup: this.ThreeEngine.getObjectByName(`四向车${car.code}`)
        });
      }
      // 情况2：正在移动到提升机位置（toCellCode 是提升机位置，positionRatio 接近 1）
      else if (car.toCellCode === cellCode && car.positionRatio > 0.9) {
        vehiclesAtElevator.push({
          code: car.code,
          vehicleGroup: this.ThreeEngine.getObjectByName(`四向车${car.code}`)
        });
      }
    });
  }
  
  return vehiclesAtElevator;
}
```

#### 2.7.4 四向车跟随提升机移动

**⚠️ 重要注意事项**：
- 在提升机和四向车一起上升下降运动时，四向车的位置**不能被它自己的位置定位所影响**
- 需要在跟随移动期间，标记四向车状态，防止 `loadFourWayVehiclePositions` 方法覆盖跟随移动的位置

```javascript
function syncVehiclesWithElevator(elevatorStatus) {
  const state = this.elevatorState;
  
  // 如果提升机位置发生变化
  if (state.currentLayer !== elevatorStatus.currentLayer) {
    const oldLayer = state.currentLayer;
    const newLayer = elevatorStatus.currentLayer;
    const newCellCode = elevatorStatus.cellCode;
    
    // 获取新位置的库位坐标
    const newLocation = this.getStorageLocationByCode(newCellCode);
    if (!newLocation) {
      console.warn(`未找到库位位置: ${newCellCode}`);
      return;
    }
    
    // 计算提升机新位置
    const newElevatorPosition = {
      x: newLocation.x,
      y: newLocation.y + this.elevatorConfig.heightOffset,
      z: newLocation.z
    };
    
    // 移动提升机
    this.moveElevatorToPosition(newElevatorPosition, newCellCode);
    
    // 查找在提升机位置的四向车（检查旧位置）
    const oldCellCode = oldLayer ? this.elevatorConfig.positions[oldLayer - 1] : null;
    if (oldCellCode) {
      const vehiclesAtElevator = this.findVehiclesAtElevator(oldCellCode);
      
      // 让四向车跟随提升机移动
      vehiclesAtElevator.forEach(vehicle => {
        if (vehicle.vehicleGroup) {
          const currentPos = vehicle.vehicleGroup.position;
          const targetY = newLocation.y + this.vehicleHeightOffset; // 四向车位置
          
          // 停止之前的动画（如果有）
          if (vehicle.vehicleGroup.tweenAnimation) {
            vehicle.vehicleGroup.tweenAnimation.stop();
          }
          
          // ⚠️ 标记四向车正在跟随提升机移动（重要！）
          vehicle.vehicleGroup.userData.isFollowingElevator = true;
          vehicle.vehicleGroup.userData.followingElevatorTargetY = targetY;
          
          // 创建跟随动画（与提升机动画同步）
          const tweenObj = { y: currentPos.y };
          vehicle.vehicleGroup.tweenAnimation = new TWEEN.Tween(tweenObj)
            .to({ y: targetY }, 2000) // 与提升机动画时间一致
            .easing(TWEEN.Easing.Quadratic.InOut)
            .onUpdate(() => {
              vehicle.vehicleGroup.position.y = tweenObj.y;
              // 标签是vehicleGroup的子对象，会自动跟随移动
            })
            .onComplete(() => {
              vehicle.vehicleGroup.position.y = targetY;
              vehicle.vehicleGroup.tweenAnimation = null;
              // ⚠️ 清除跟随标记（重要！）
              vehicle.vehicleGroup.userData.isFollowingElevator = false;
              vehicle.vehicleGroup.userData.followingElevatorTargetY = null;
              console.log(`四向车 ${vehicle.code} 已跟随提升机移动到第 ${newLayer} 层`);
            })
            .start();
        }
      });
    }
    
    // 更新状态
    state.currentLayer = newLayer;
  }
}
```

#### 2.7.5 防止位置更新冲突

在 `loadFourWayVehiclePositions` 方法中，需要检查四向车是否正在跟随提升机移动：

```javascript
// 在 loadFourWayVehiclePositions 方法中，更新四向车位置时
allCarList.forEach((car) => {
  const carCode = car.code;
  const vehicleName = `四向车${carCode}`;
  let vehicleGroup = this.ThreeEngine.getObjectByName(vehicleName);
  
  // ... 查找四向车对象的代码 ...
  
  // ⚠️ 检查四向车是否正在跟随提升机移动
  if (vehicleGroup && vehicleGroup.userData.isFollowingElevator) {
    // 如果正在跟随提升机移动，只更新X和Z坐标，Y坐标由跟随动画控制
    const position = this.calculateVehiclePosition(
      car.fromCellCode,
      car.toCellCode,
      car.positionRatio
    );
    
    if (position) {
      // 只更新X和Z坐标，保持Y坐标由跟随动画控制
      vehicleGroup.position.x = position.x;
      vehicleGroup.position.z = position.z;
      // Y坐标不更新，由跟随动画控制
      
      console.log(`四向车 ${carCode} 正在跟随提升机移动，只更新X和Z坐标`);
      return; // 跳过后续的位置更新逻辑
    }
  }
  
  // 正常的位置更新逻辑（如果不在跟随状态）
  // ... 原有的位置更新代码 ...
});
```

### 2.8 实现步骤

#### 步骤1：初始化配置
在 `data()` 中定义提升机配置和状态

#### 步骤2：实现需求一（路径终点检测）
在 `loadCarPaths` 方法中，处理完路径数据后：
1. 调用 `checkPathDestination(pathList)` 检测每条路径的终点
2. 收集所有终点是提升机位置的路径
3. 调用 `syncElevatorWithPaths(pathGroups)` 同步提升机

#### 步骤3：实现需求二（跟随移动）
1. 创建 `loadElevatorStatus(elevatorId)` 方法请求后端接口
2. 创建定时器，定期请求提升机状态（建议每1-2秒）
3. 实现 `findVehiclesAtElevator()` 检测四向车是否在提升机位置
4. 实现 `syncVehiclesWithElevator()` 让四向车跟随提升机移动

#### 步骤4：集成到现有代码
1. 在 `loadCarPaths` 方法的最后，调用 `syncElevatorWithPaths(pathGroups)`
2. 在组件挂载时启动提升机状态更新定时器
3. 在组件销毁时清理定时器

### 2.5 边界情况处理

1. **四向车快速通过提升机位置**
   - 如果 `positionRatio` 变化太快，可能检测不到
   - 解决方案：记录上一次的位置，如果从非提升机位置直接跳到另一个非提升机位置，中间经过提升机位置，也要检测

2. **提升机正在移动中，又有新的四向车到达**
   - 解决方案：将新到达的四向车加入等待队列，提升机移动完成后根据优先级选择下一个目标

3. **多个四向车同时到达同一层**
   - 解决方案：使用队列管理，先来先服务，或者根据四向车的任务优先级

4. **提升机模型不存在**
   - 解决方案：只更新状态，不执行动画，记录警告日志

5. **⚠️ 位置更新冲突（重要！）**
   - **问题**：四向车跟随提升机移动时，`loadFourWayVehiclePositions` 方法可能会覆盖跟随移动的位置
   - **解决方案**：
     - 使用 `vehicleGroup.userData.isFollowingElevator` 标记四向车正在跟随提升机移动
     - 在跟随期间，`loadFourWayVehiclePositions` 只更新X和Z坐标，不更新Y坐标
     - Y坐标完全由跟随动画控制
     - 跟随动画完成后，清除标记，恢复正常位置更新

## 三、技术细节

### 3.1 动画同步
- 四向车移动动画：2秒
- 提升机移动动画：2秒（保持一致）
- 使用 TWEEN.js 确保动画平滑

### 3.2 性能优化
- 只在四向车位置更新时检测，避免每帧都检测
- 使用增量更新，只处理变化的四向车
- 提升机状态缓存，避免重复计算

### 3.3 调试支持
- 添加详细的日志输出
- 可以在控制台查看提升机状态
- 可视化提升机当前位置和目标位置

## 四、扩展性考虑

### 4.1 多提升机支持
- 配置结构支持多个提升机
- 每个提升机独立管理状态
- 可以轻松添加新的提升机配置

### 4.2 动作类型扩展
- 当前支持：载货、卸货、等待
- 可以扩展：充电、维修、待机等

### 4.3 优先级策略
- 当前：最近距离优先
- 可以扩展：任务优先级、紧急程度、等待时间等

## 五、待确认问题

1. **提升机对象名称**：模型中提升机对象的实际名称是什么？（如："提升机1"、"elevator-1"、"载货台"等）
2. **提升机移动方式**：提升机是整体移动还是只有载货台移动？移动的是哪个对象？
3. **高度偏移量**：`heightOffset` 的初始值应该是多少？（建议从 `-2.0` 开始，可根据实际情况调整）
4. **多个四向车处理**：如果多个四向车的路径终点是不同层，提升机应该移动到哪一层？（当前方案：移动到最后一个请求的层）
5. **动画时间**：提升机移动动画时间是否与四向车一致（2秒）？
6. **初始化位置**：提升机的初始位置在哪里？是否需要从当前位置移动到目标位置？

## 六、实施计划

1. **第一阶段**：基础检测和状态管理
   - 实现位置检测
   - 实现状态管理
   - 添加日志输出

2. **第二阶段**：提升机动画
   - 实现提升机移动动画
   - 同步动画时间

3. **第三阶段**：多车处理
   - 实现队列管理
   - 实现优先级策略

4. **第四阶段**：优化和测试
   - 性能优化
   - 边界情况处理
   - 完整测试

---

**请审核以上方案，确认以下信息后开始实施：**
1. 提升机对象名称
2. 动作触发时机
3. 优先级策略
4. 其他特殊需求

